{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.10.1"
    },
    "scripts": {
      "reset": "lerna clean -y && rm -rf node_modules && yarn",
      "start": "cd apps/app-foo && yarn run start" 
    },
    "workspaces": [
        "apps/*",
        "components/*"
    ]
}
